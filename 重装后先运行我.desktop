[Desktop Entry]
Type=Application
Version=1.1
Name=重装后先运行我（一键重建环境）
Name[en]=Run me first after SteamOS reinstall
Comment=双击运行 steamos-setup.sh：自动提权、断点续传、逐个重建重装后的环境
# %k = 本 .desktop 文件的路径 → 用它定位同目录的启动脚本, 备份包放哪都能用。
# 本文件刻意只当薄壳: 真正的逻辑都在同目录的 `重装后先运行我.sh`, 只写一份。
# ⚠️ 两条别改回去的约束:
#   · 不写 TryExec=konsole —— 那样在没装 konsole 的机器上**整个入口会消失**
#     (TryExec 找不到就被隐藏, 双击也无效)。命令本身是 shell 脚本, 不该拿终端去卡它。
#   · 用 Terminal=true 而不是在 Exec 里硬写 konsole —— 让桌面环境自己挑默认终端,
#     少一个硬依赖, 以后换终端也不受影响。
# ⚠️ 从 Windows / 网盘 / U 盘 拷回来会丢执行位, 双击会没反应 → 先 chmod +x *.sh *.desktop
Exec=/bin/bash -c 'd="$(dirname "%k")"; cd "$d" 2>/dev/null || cd "$HOME/Downloads/steamos-reinstall-backup" || exit 1; exec bash ./重装后先运行我.sh'
Terminal=true
Icon=steamos-runme
Categories=System;
