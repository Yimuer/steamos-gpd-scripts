# SteamOS 重装全流程（GPD Win5 / AMD 掌机·台式机）

> 一页纸流程。细节与排错见 `使用说明.txt`，历史变更见 `CHANGELOG.md`。

---

## 阶段 0 · 重装前准备

- [ ] **备份所有要保留的数据**——重装会清空目标硬盘（游戏存档、截图、非 Steam 目录等）
- [ ] 启动U盘已就绪（SteamOS 恢复镜像已写入）
- [ ] 备份包（本文件夹）已拷到手机/移动盘/网盘——**重装会清掉盘上的旧备份**

## 阶段 1 · 重装系统

1. U盘插上，开机进 BIOS：**关闭 Secure Boot**，启动项选 U盘（UEFI）
2. 进入 SteamOS 恢复桌面（需要接键盘鼠标），双击桌面 **"Reimage Steam Deck"**
3. 等脚本跑完关机，**拔掉U盘**
4. 开机进入 SteamOS 引导设置：语言 → Wi-Fi → 登录 Steam 账号
5. 首次启动较慢是正常的（分区扩展）

## 阶段 2 · 首次进入桌面

1. Steam 键 → 电源 → **切换至桌面**
2. 先给用户设个密码（后面所有 sudo 都要用）：
   Konsole 终端里执行 `passwd`，设一个好记的密码
3. 把备份包解压到 `~/Downloads`（推荐，其他位置也行）

## 阶段 3 · 重建环境（核心步骤）

1. 打开解压出的文件夹，双击 **「重装后先运行我」**
   （若提示"不受信任的启动器"：右键 → 属性 → 勾选信任；或改用备用 `.sh`）
2. 输入密码，脚本自动全量重建，中途可随意中断——**再双击一次会从断点继续**
3. 十二步跑完显示"全部完成"即成功；有红色报错时截图排查
   3b. 跑完会询问"是否安装可选组件(微信等)"——按需确认，随时可单独双击
       `可选组件安装.sh` 补装（台式机装微信同此入口）
4. **注销并重新登录**（使部分配置生效）

## 阶段 4 · 验收清单

- [ ] `bash steamos-setup.sh --status` 显示各步已完成
- [ ] 游戏模式里 "…" 菜单出现 **Decky 图标**，插件有 SteamGridDB / ProtonDB Badges / SimpleDeckyTDP
- [ ] GPD Win5 背键（L4/R4）可用
- [ ] 终末地能进游戏（走 DW-Proton）；鸣潮不黑屏
- [ ] 开机速度快（NTP 已换境内源）

## 日后 · 系统版本升级（3.8 → 3.9 …）

- 升级后**首次开机**：自愈钩子自动清点被冲掉的内容并弹通知
- 若提示需要手动（免密文件也被冲了），跑一条命令即可：
  ```bash
  sudo bash ~/Downloads/steamos-reinstall-backup/steamos-setup.sh --after-upgrade
  ```
- 之后系统会全自动恢复，无需人工介入

## 常见问题速查

| 症状 | 处理 |
|---|---|
| rootfs 空间告急 | `sudo bash free-rootfs.sh`（先体检，`--apply` 执行） |
| 终末地闪退 | 兼容性必须选 **DW-Proton**（`install-dwproton.sh`） |
| Decky 图标不出现 | 完全退出 Steam 重开；还没好跑 `sudo bash steamos-setup.sh 5` |
| 其他 | 查 `使用说明.txt` 备用脚本区，或 `bash check.sh` 自检项目完整性 |
